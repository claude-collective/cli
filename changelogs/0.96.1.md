# Release 0.96.1 (2026-03-29)

## Changed

- Info panel now shows compact numeric stats instead of listing all skill names — Skills and Agents counts broken down by scope (global/project) and install mode (plugin/eject), delegating to `computeStats` from `stats-panel.js` (D-144)
- Info panel layout fixed to properly fill terminal height and include the footer hotkey bar when the panel is open
