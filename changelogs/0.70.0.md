# Release 0.70.0 (2026-03-11)

## Changed

- **Centralize remaining test data (R-09b)** — added pre-built matrix constants (`EMPTY_MATRIX`, `SINGLE_REACT_MATRIX`, `WEB_PAIR_MATRIX`, `FULLSTACK_PAIR_MATRIX`, `ALL_SKILLS_*` variants) to `mock-matrices.ts`, replacing inline `createMockMatrix(SKILLS.*)` calls across 28 test files
- Added `buildSourceConfig()` factory to helpers.ts for `ProjectSourceConfig` test objects
- Replaced inline `ProjectConfig`/`AgentScopeConfig` construction with `buildProjectConfig()`/`buildAgentConfigs()` factories
- Replaced inline agent metadata strings with `AGENT_DEFS` from `mock-agents.ts`
- Updated CLAUDE.md with NEVER/ALWAYS rules for matrix constants, config factories, and agent defs
- Updated test count to 3319+
- Moved 19 completed tasks from TODO.md, TODO-deferred.md, and TODO-refactor.md to TODO-completed.md
