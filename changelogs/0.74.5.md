# Release 0.74.5 (2026-03-13)

## Fixed

- **stacks**: Initialize matrix before stack resolution — `getCustomSkillIds()` now sees custom skills during `resolveAgentConfigToSkills` validation, eliminating false "Invalid skill ID" warnings for custom skills like `engine-image`
- **matrix**: Default synthesized categories to non-exclusive — auto-synthesized categories (e.g., "engine") now allow multi-select; source authors can set `exclusive: true` explicitly in `skill-categories.ts` if needed
