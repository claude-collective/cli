# Release 0.103.0 (2026-04-02)

## Fixed

- Loading spinner now shows during source fetch for both `init` and `edit` commands, replacing the blank terminal hang during network loads
- E2E tests: replaced 8 `try/finally` blocks with `beforeEach`/`afterEach` across 3 files, moved temp dir creation to `beforeEach` so test bodies don't mutate shared variables
- E2E tests: removed inner `try/catch` wizard cleanup blocks in favor of `afterEach` cleanup
- Removed "Test N:" numbering prefixes from 9 E2E test names across 5 lifecycle files

## Changed

- Migrated 4 `toggleSkill()` calls to `selectSkill()` in E2E tests — `selectSkill` navigates to the correct grid position before toggling, `toggleSkill` just pressed Space on whatever was focused
- Removed the deprecated `toggleSkill` method from `BuildStep` page object
- Marked D-165 (type-system issues) as done — all 4 items resolved across prior sessions
- Updated E2E standards docs (patterns, anti-patterns, page-objects) to reflect `selectSkill` migration
- Applied Prettier formatting to `edit.tsx` and `init.tsx`
