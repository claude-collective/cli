# Release 0.82.3 (2026-03-24)

## Fixed

- S hotkey (scope toggle) no longer fires when editing from global scope (`~/.claude/`) — toggling to project scope is undefined without a project context
- Scope footer hint hidden when editing from global scope
- Help modal no longer shows S key entry when editing from global scope
- Store-level safety guards in `toggleSkillScope` and `toggleAgentScope` prevent scope changes in global context
