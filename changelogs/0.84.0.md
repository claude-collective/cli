# Release 0.84.0 (2026-03-25)

## Changed

- Restructured all AI documentation under `.ai-docs/` with `reference/`, `standards/`, and `agent-findings/` subdirectories — consolidates docs that were spread across `.agents-docs/`, `.ai-docs/` root, `docs/standards/`, and `docs/guides/`
- Renamed `scribe` agent to `codex-keeper` across the entire codebase — agent partials, generated types, default stacks, wizard UI, integration tests, and all agent workflow references
- Updated README with current 16-stack catalog, expanded 154-skill listing across 8 domains, and simplified config commands table
- Updated all internal path references in CLAUDE.md, convention-keeper agent, docs/index.md, and DOCUMENTATION_MAP.md to reflect new directory structure

## Added

- D-145 operations layer specification and implementation plan
- D-146 SkillId pattern replacement task
