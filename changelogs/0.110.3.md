# Release 0.110.3 (2026-04-06)

## Fixed

- Confirm step no longer shows deselected global skills as "removed from global" during init — added `isInitMode` flag to suppress global removal markers when not editing an existing installation

## Removed

- `cc info` command — unused command that showed skill metadata; skill details are accessible through the wizard and marketplace directly
