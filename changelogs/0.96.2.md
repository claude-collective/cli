# Release 0.96.2 (2026-03-29)

## Changed

- Replaced `try/finally` cleanup blocks in E2E test bodies with `afterEach` hooks across 8 lifecycle and interactive tests — `global-scope-lifecycle`, `config-scope-integrity`, `dual-scope-edit-display`, `dual-scope-edit-mixed-sources`, `dual-scope-edit-source-changes`, `source-switching-per-skill`, `real-marketplace` (D-166)
- Moved task IDs from `describe()` block names to JSDoc comments in `init-wizard-default-source`, `init-wizard-sources`, `init-wizard-plugin` (D-167)
- Fixed `undefined` reset to `undefined!` in `search-interactive` afterEach cleanup (D-167)

## Fixed

- `eject.ts`: Replaced `skipReason!` non-null assertions with discriminated union result types on `EjectAgentPartialsResult` and `EjectSkillsResult` (D-165)
- `search.tsx`: Captured `options.category` in local const before closure entry to prevent TypeScript narrowing loss (D-165)
- `doctor.ts`: Destructured `result.details` before use to eliminate non-null assertion after intermediate boolean check (D-165)
