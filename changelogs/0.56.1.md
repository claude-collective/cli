# Release 0.56.1 (2026-03-02)

## Changed

- `writeSourceSkill()` test helper now requires explicit `domain` parameter instead of inferring from category prefix
- JSDoc comments on `domain` fields in `AgentDefinition`, `AgentYamlConfig`, `SkillMetadataConfig`, and `metadataValidationSchema` updated to remove "overrides inference" language

## Docs

- D-37 install mode redesign doc updated for config.ts format (was config.yaml)
- Dropped `skillOverrides` proposal — `sourceSelections` already captures per-skill choices
- `installMode` is now derived from `sourceSelections`, not stored separately
- Resolved open questions Q5, Q7; updated Q4 confirmation to use Ink
