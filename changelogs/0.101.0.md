# Release 0.101.0 (2026-04-02)

## Added

- `new:agent --force` flag: prevents silent overwrites by checking for existing agent directory before invoking agent-summoner, matching the existing `new:skill` pattern

## Fixed

- `eject` command: replaced `sourceResult!` non-null assertions with guard clauses for type safety
- cli-reviewer agent: added missing findings capture instruction (matching infra-reviewer)
- ai-developer agent: removed stale `.claude/progress.md` path reference from playbook

## Changed

### Test quality improvements

- Replaced `toEqual` with `toStrictEqual` across all 57 test files (497 replacements) — enforces stricter object comparison per CLAUDE.md
- Removed 9 unnecessary `undefined!` assignments from E2E afterEach blocks across 6 files
- Centralized per-test console spy boilerplate into beforeEach/afterEach across 7 files (~45 patterns)
- Removed unnecessary mocks from plugin-settings.test.ts (getErrorMessage, consts, logger) and step-settings.test.tsx (DEFAULT_SOURCE)

### Documentation

- Updated all reference docs (commands, architecture, dependency graph, wizard, components, stores, state transitions, skills-matrix, type-system, utilities, config, plugin, agent, compilation, operations, boundary-map, test-infrastructure)
- Audited and fixed all standards documentation
- Fixed findings-impact-report factual errors
- Updated DOCUMENTATION_MAP with 15-pass validation history

### Agent findings audit

- Audited all 90 agent findings across 8 categories
- 41 findings were already fixed in prior sessions
- 15 findings fixed in this release (test quality, CLI bugs, agent definitions)
- 36 findings verified as false positives (auditing agent hallucinated "correct" APIs)
- Removed all 90 finding files after audit completion

### Style

- Applied Prettier formatting across docs, components, and tests
- Fixed wizard tab current-step text color from UNFOCUSED to black for contrast
