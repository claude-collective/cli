# 0.34.0

## Wizard UX Overhaul

### Layout and Branding

- Move ASCII art logo from stdout into Ink component tree so it stays fixed at the top
- Reposition marketplace label directly below the logo
- Constrain wizard layout to terminal height on all steps (not just build)

### Build Step

- Replace virtual scroll with pixel-accurate `measureElement`-based scrolling in CategoryGrid
- Add stable initial sort order: selected > recommended > normal > discouraged > disabled
- Cache sort order per category so toggling selections does not reorder skills
- Remove LegendRow and UI symbol icons from skill tags
- Make disabled skills hoverable (focusable) but not toggleable
- Add `flexBasis={0}` to build step containers for proper flex measurement

### Domain Selection

- Rewrite domain selection with custom `useInput` replacing `@inkjs/ui` Select
- Add wrapping navigation (up past first wraps to last, down past last wraps to first)
- Separate Enter (continue) from Space (toggle domain)
- Remove Back button — ESC handles back navigation

### ViewTitle Component

- Redesign as three-line yellow background block with vertical and horizontal padding
- Narrow `children` prop from `ReactNode` to `string` for reliable width calculation
- Add step numbers to titles: `[1] Choose a stack`, `[2] Customize your Web stack`

### Wizard Tabs

- Remove circle/tick icons from step tabs (just `[1] Stack`, `[2] Build`, etc.)
- Remove branding name display from tab bar
- Narrow `WizardTabStep.id` and props from `string` to `WizardStep` union type

## Code Quality

- Narrow `StatusMessage.color` to `"red" | "green"` at source, removing downstream cast
- Add `verbose()` logging to silent catch in step-settings
- Fix missing `.js` extension on consts import in wizard-store
- Narrow `getStepProgress` return type to `WizardStep[]`
- Remove dead fallback in `getDomainDisplayName`
- Replace `warn()` with `verbose()` in source-loader marketplace fallback
- Extract `TITLE_HORIZONTAL_PADDING` constant in view-title
