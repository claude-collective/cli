# Release 0.113.0 (2026-04-08)

## Fixed

- **Globally installed skills can no longer be deselected from project scope** — `toggleTechnology` now guards against toggling globally-installed skills when editing from project scope. Shows toast: "Global skills cannot be changed from project scope". Init mode is exempt so preselected global skills can still be adjusted during first project setup.

## Added

- E2E lifecycle test verifying the global skill toggle guard from project scope
- Agent finding documenting the missing scope guard in `toggleTechnology`
