# Release 0.81.1 (2026-03-23)

## Fixed

- Source-provided skill rules now override defaults when both reference the same slug. Previously, the default `recommends` reason always won because default rules were spread first in the merged array and `.find()` returned the first match. Source rules are now placed before defaults in all relationship arrays.
