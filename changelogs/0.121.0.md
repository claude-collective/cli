# Release 0.121.0 (2026-04-13)

## Added

- Scope labels `[G]`/`[P]` on added and removed skills in the `edit` command change summary (D-197)
- G→P scope changes now render as additions (`+`, green) instead of modifications (`~`, yellow)
- 13 new unit tests for `detectConfigChanges` covering added/removed skills, scope changes, source changes, and agent changes

## Changed

- Source and scope change lines in the change summary now show skill display names instead of raw skill IDs
- Exported `ConfigChanges` type and `detectConfigChanges` function for testing
