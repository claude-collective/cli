# Release 0.110.1 (2026-04-06)

## Fixed

- Consolidated duplicate `buildCompileConfig` into `buildCompileAgents` — single canonical path for compile-agent construction with D7 cross-scope safety filtering (global agents can no longer receive project-scoped skills during recompilation)

## Removed

- Dead `buildCompileConfig`, `BuildCompileConfigParams`, `BuildCompileConfigResult` types from agent-recompiler
