# Release 0.82.2 (2026-03-23)

## Fixed

- E2E source creates skills in flat directory structure (was incorrectly nesting under category)
- `diff` and `outdated` E2E tests no longer hardcode category as a path segment
- Domain subnav hoisted outside tab bar when domains exceed character threshold (Yoga absolute positioning workaround)
- Confirm step no longer appends domain list to "Ready to install" text

## Changed

- TODO table descriptions shortened for readability — detail moved to expanded sections below
- D-100, D-133, D-134, D-139 marked as done
- Added task specs for D-141 (merge AI docs), D-142 (rename scribe to codex-keeper), D-143 (stack descriptions)

## Removed

- 34 `undefined!` non-null assertions across 30 E2E test files
- 11 raw `readFile` calls replaced with `readTestFile()` from test-utils
