# Release 0.69.0 (2026-03-11)

## Changed

- **`loadStackById` checks default stacks internally (R-01)** — callers no longer need to duplicate the `defaultStacks` fallback. The function now tries source stacks first, then falls back to CLI built-in defaults automatically.
- Removed duplicated fallback logic from `local-installer.ts` and `stack-plugin-compiler.ts`

## Added

- R-07 implementation plan: codegen `SkillId`, `SkillSlug`, `Category`, `Domain`, `AgentName` from source metadata
- R-01 implementation plan
