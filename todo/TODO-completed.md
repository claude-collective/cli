# Agents Inc. CLI - Completed Tasks

> Tasks moved here from [TODO.md](./TODO.md), [TODO-deferred.md](./TODO-deferred.md), and [TODO-refactor.md](./TODO-refactor.md) after completion.

---

## From TODO.md

| ID    | Task                                                                                                                                                                                                                                                                                                |
| ----- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D-127 | UX for claiming global skills/agents into project scope — closed: users can now toggle global skills to project scope directly in the build/agents step via the scope hotkey                                                                                                                        |
| D-188 | Bug: `cc list` shows all skills and agents as "just added" — fixed by setting wizard store baseline to current configs before rendering, so all items show neutral status                                                                                                                           |
| D-125 | Fix weak E2E assertions — scope-blind `\|\|` checks removed from source-switching-modes and init-then-edit-merge; category display name assertions in edit-wizard-detection are stable category headers, not fragile                                                                                |
| D-165 | Fix 4 type-system critical issues from D-138 audit — sourceResult! fixed with guard clauses, oclif boundary casts documented as acceptable, marketplace.ts casts documented as code-gen boundary                                                                                                    |
| D-166 | Fix E2E try/finally blocks — replaced 8 finally blocks and 2 try/catch blocks with beforeEach/afterEach across 3 files, moved temp dir creation to beforeEach so test bodies don't mutate shared variables                                                                                          |
| D-150 | Migrate E2E tests from toggleSkill to selectSkill — 4 call sites migrated, deprecated toggleSkill method removed from BuildStep, E2E standards docs updated                                                                                                                                         |
| D-176 | Migrate CLI_COLORS to hex values only — all named ANSI colors replaced with hex in CLI_COLORS object, all inline color strings routed through CLI_COLORS, chalk calls migrated to chalk.hex()                                                                                                       |
| D-175 | Bug: Double curlies stripped from agent-summoner, pattern-scout, infra-reviewer during compile — stopped sanitizing content sections (identity, playbook, output, critical-requirements, critical-reminders) in `sanitizeCompiledAgentData()`, keeping metadata sanitization intact                 |
| D-174 | Bug: Terminal too-narrow message is static — added `useState` + `useEffect` resize listener in `wizard.tsx` so the warning appears/disappears reactively when the terminal is resized                                                                                                               |
| D-167 | Remove task IDs from describe() blocks in init-wizard test files — already done; no task IDs remain in any describe() strings across E2E tests                                                                                                                                                      |
| D-171 | Style edit command output — green added, red removed, white titles, gray body, proper spacing — implemented with chalk in `edit.tsx`: green `+` added, red `-` removed, yellow `~` changed, white bold title, gray annotations                                                                      |
| D-169 | Improve `list` command — show all installed skills and agents — now renders `SkillAgentSummary` Ink component with scope-grouped skills/agents, matching the info panel visual style                                                                                                                |
| D-156 | Rename "local mode" to "eject mode" — marked done                                                                                                                                                                                                                                                   |
| D-159 | Lock badge + toast for locked global skills in project-scope edit — marked done                                                                                                                                                                                                                     |
| D-144 | Info panel — replace `?` overlay with `I` panel (stats, context, toggles) — marked done                                                                                                                                                                                                             |
| D-112 | Write "Using the codex-keeper subagent" guide — written at `docs/guides/using-codex-keeper.md`                                                                                                                                                                                                      |
| D-161 | Exhaustive categories allow deselecting locked global skills via radio swap in edit mode — exclusive toggle now rejects swap when a locked skill is selected (0.94.0)                                                                                                                               |
| D-160 | Sources step only shows check for current-scope skills — global plugin discovery added to `collectPluginSkillIds`, matching global+project merge pattern (0.94.0)                                                                                                                                   |
| D-163 | Init skips global agent compilation — fixed: init now uses multi-pass compilation matching `compile.ts` pattern                                                                                                                                                                                     |
| D-158 | Add loading spinner to --refresh flag — spinner now shows during all source loading (init + edit), not just --refresh. Uses existing Spinner component from common/spinner.tsx                                                                                                                      |
| D-123 | Local mode ENOENT — `sourcePath` now populated via `fetchFromSource()` for BUILT_IN_MATRIX; `acceptStackDefaults()` E2E helper added for default-source flow (0.93.0)                                                                                                                               |
| D-124 | E2E tests for default source path — 4 tests passing: marketplace registration, stale update, full init with BUILT_IN_MATRIX, wizard load verification (0.93.0)                                                                                                                                      |
| D-154 | Organize operations into domain subfolders — reorganized 15 ops into `source/`, `skills/`, `project/`; deleted dead `executeInstallation`/`recompileProject` (0.93.0)                                                                                                                               |
| D-152 | Fix ENOENT in dual-scope skill copy — `localPath` changed from relative to absolute at extraction time, eliminating cwd/projectDir mismatch (0.93.0)                                                                                                                                                |
| D-157 | Eliminate hardcoded marketplace data — resolved: default-categories/rules/stacks are intentional exhaustive defaults with source override mechanism, not a problem                                                                                                                                  |
| D-97  | Improve startup time — lazy-load matrix generation — resolved: `BUILT_IN_MATRIX` pre-built artifact already makes normal startup fast; only `--refresh` is slow (network-bound, inherent)                                                                                                           |
| D-92  | Global config missing `source`, `marketplace`, `selectedAgents` on init — fixed in `splitConfigByScope` via `...config` spread (bd55731)                                                                                                                                                            |
| D-155 | Declarative commands — two-tier orchestrator for all commands (0.87.0)                                                                                                                                                                                                                              |
| D-149 | Stop inferring metadata from names/paths (0.88.0)                                                                                                                                                                                                                                                   |
| D-148 | Remove unnecessary test mocks in 3 plugin test files (0.88.0)                                                                                                                                                                                                                                       |
| D-145 | Operations layer — centralize repeated command sequences (0.86.0)                                                                                                                                                                                                                                   |
| D-132 | Skip incompatibility markers in exclusive (radio) categories                                                                                                                                                                                                                                        |
| D-129 | Add visibility into global config contents from project config                                                                                                                                                                                                                                      |
| D-116 | Filter Incompatible toggle should also deselect incompatible skills                                                                                                                                                                                                                                 |
| D-146 | Replace `SKILL_ID_PATTERN` regex with generated data — replaced hardcoded domain regex with `SKILL_IDS`/`CATEGORIES` arrays, `hasSkill()`, removed redundant format checks (0.84.1)                                                                                                                 |
| D-142 | Rename `scribe` → `codex-keeper`, keep `convention-keeper` (0.84.0)                                                                                                                                                                                                                                 |
| D-130 | Narrow stack type safety — category-scoped SkillId unions via `generateStackAgentConfig()` in `config-types-writer.ts`                                                                                                                                                                              |
| D-141 | Merge AI docs — consolidate `.ai-docs/` with `reference/` and `standards/` split (0.84.0)                                                                                                                                                                                                           |
| D-119 | Update READMEs with current stats (154 skills, 16 stacks) (0.84.0)                                                                                                                                                                                                                                  |
| D-107 | Audit base framework skills for meta-framework conflicts — added skip directives to React (useActionState) and Vue (watch+fetch) SKILL.md                                                                                                                                                           |
| D-114 | Create `infra` domain — cancelled, sticking with `shared-` prefix for infra/monorepo/ci-cd/security skills                                                                                                                                                                                          |
| D-104 | Audit all skills against the Skill Atomicity Bible — all skills audited and compliant                                                                                                                                                                                                               |
| D-91  | `uninstall --all` filters to CLI-installed plugins only — uses `${id}@${source}` heuristic against config skills (106f79a)                                                                                                                                                                          |
| D-103 | Remove `tags` from skill metadata — removed from schemas, types, loaders, resolvers, search, info command, all tests (0.75.1)                                                                                                                                                                       |
| D-108 | Add `$schema` reference to generated metadata.yaml in `cc new skill` — uses custom-metadata.schema.json (0.75.1)                                                                                                                                                                                    |
| D-101 | Fix "Next.js Fullstack" stack description — updated to "Next.js + Hono full-stack" (0.75.1)                                                                                                                                                                                                         |
| D-38  | Split framework categories — created `web-meta-framework` category, restructured conflict/requires/alternatives rules (released in 0.75.0)                                                                                                                                                          |
| D-39  | Couple meta-frameworks with base frameworks — required-by label, block-deselect, auto-select (released in 0.75.0)                                                                                                                                                                                   |
| D-102 | Merge Next.js App Router + Server Actions into single unified skill with progressive disclosure (released in 0.75.0)                                                                                                                                                                                |
| D-94  | Stack change or "start from scratch" doesn't reset previously selected skills                                                                                                                                                                                                                       |
| D-95  | Create a reusable view title component for wizard steps                                                                                                                                                                                                                                             |
| D-96  | Remove redundant left/right arrow navigation description below views                                                                                                                                                                                                                                |
| D-98  | Break up large E2E test files + restructure E2E folder organization                                                                                                                                                                                                                                 |
| D-99  | Update README + .ai-docs to reflect current architecture                                                                                                                                                                                                                                            |
| D-101 | Add `compatibleWith` groups to default-rules.ts — React, Vue, Angular, multi-framework scoping for 25+ framework-specific skills                                                                                                                                                                    |
| D-105 | Split `infra-tooling-setup-tooling` into 4 atomic skills + new `web-tooling` category — eslint-prettier, typescript-config, git-hooks, vite                                                                                                                                                         |
| D-106 | Inline methodology skills into Liquid agent template — 6 partials in `_templates/methodologies/`, removed `SHARED_METHODOLOGY` from stacks                                                                                                                                                          |
| D-113 | Stack selection → compact flat list — chevron pointer, inline label + dimmed description, React/Other Frameworks groups, `useRowScroll`                                                                                                                                                             |
| D-115 | Overhaul default stacks — 13 stacks total (4 new), framework-first naming, "Full-Stack" suffix, comma-separated descriptions, React-first order                                                                                                                                                     |
| D-133 | E2E tests for 13 untested bug fixes — compile-scope-filtering.e2e.test.ts (7 tests: dual-pass, global plugin discovery, clobber prevention) + config-scope-integrity.e2e.test.ts (5 tests: source priority, agent scope merge, old agent deletion, stack filtering, domain placement, domain types) |
| D-135 | Edit-to-local flow writes agents to real HOME despite HOME override — fixed in v0.79.0 with scope-aware agent routing via `agentScopeMap`                                                                                                                                                           |
| D-137 | Standards feedback loop — capture and synthesize anti-pattern findings via `.ai-docs/agent-findings/` pipeline and convention-keeper agent                                                                                                                                                          |
| D-134 | Declarative E2E test utilities — high-level helpers replacing imperative patterns                                                                                                                                                                                                                   |
| D-93  | Global-scoped plugins double-installed to both project and global settings.json — fixed with scope-aware plugin installation                                                                                                                                                                        |
| D-139 | Skill domain & category reorganization                                                                                                                                                                                                                                                              |
| D-121 | Remove step numbers from wizard tabs — cleaner `Stack  Domains  Skills  Sources  Agents  Confirm` (0.81.0)                                                                                                                                                                                          |
| D-120 | Add dedicated domain selection step to wizard flow (0.81.0)                                                                                                                                                                                                                                         |
| D-126 | Global uninstall skips local skills — `forkedFrom` metadata check fails, fixed with scope-aware uninstall                                                                                                                                                                                           |
| D-143 | Shorten default stack descriptions — drop redundant framework names from descriptions                                                                                                                                                                                                               |
| D-100 | Fix pre-existing E2E test violations                                                                                                                                                                                                                                                                |
| D-117 | Show selected global/project skill counts in wizard UI                                                                                                                                                                                                                                              |
| D-128 | Disable scope toggle (S hotkey) when editing from global scope — no-op + hidden footer/help hints                                                                                                                                                                                                   |
| D-140 | Agent gap analysis — add 5 new agents (ai-developer, api-tester, api-pm, ai-reviewer, infra-reviewer) (0.92.0)                                                                                                                                                                                      |
| D-181 | Bug: New global skills added during project init not written to global config — need merge, not skip                                                                                                                                                                                                |
| D-184 | Sources step: separate global and project skills/agents into labelled sections                                                                                                                                                                                                                      |
| D-185 | Fix E2E: `dual-scope-edit-display` — resolved: inlined config now includes global stack                                                                                                                                                                                                             |
| D-186 | Fix E2E: `global-scope-lifecycle` doctor test — resolved: same root cause as D-185                                                                                                                                                                                                                  |
| D-187 | Disable scope toggle (S key) when running init/edit from HOME — show toast explaining why                                                                                                                                                                                                           |
| D-179 | Bug: `init` in new project ignores global installation — `loadGlobalConfigIfExists()` feeds global skills as `globalPreselections`, merged at stack/scratch selection                                                                                                                               |
| D-183 | Propagate global skill changes to all known project configs — `propagateGlobalChangesToProjects()` iterates registered projects, updates config-types.ts and config.ts                                                                                                                              |
| D-131 | Track project installations in global config — `projects?: string[]` in global config, `registerProjectPath()`/`deregisterProjectPath()` in local-installer                                                                                                                                         |
| D-180 | Bug: Project init overwrites global config-types.ts — fixed in `403df46`: project-level operations no longer modify global config, both files gated behind `needsGlobalWrite`                                                                                                                       |
| D-182 | Bug: Confirm step diff — deselected global skills showed as removal from global during init. Fixed with `isInitMode` flag suppressing global removal markers. Scope change part fixed earlier.                                                                                                      |
| D-178 | Bug: Scope toggle (S hotkey) blocked by excluded tombstones — `toggleSkillScope` guards didn't filter `excluded: true` entries. Fixed with `!sc.excluded` on both checks. ENOENT was separately fixed by D-152.                                                                                     |
| D-177 | Style skill select: left border only, change category colour — prototyped row layout with vertical divider, decided against it                                                                                                                                                                      |
| D-164 | Improve confirm step UI — current two-column scope-grouped layout with diff markers is the correct design, no changes needed                                                                                                                                                                        |
| D-175 | Scope-pure project config — `ProjectAgentName` type narrows to project-scoped agents only, project config stack filtered to exclude global agents, config-types-writer generates scope-aware unions                                                                                                 |
| D-199 | Bug: P→G toggle doesn't clear excluded tombstone — fixed: `toggleSkillScope` now checks for existing excluded entry before blocking; allows undo when tombstone exists from prior G→P toggle                                                                                                        |
| D-198 | Bug: Project skill misreported as global — fixed: `buildSkillConfigForId` now prefers project-scoped entry over global when duplicates exist in savedConfigs                                                                                                                                        |
| D-197 | Change summary: display names, scope labels, G→P as addition — `logChangeSummary` now uses `getSkillById().displayName`, adds `[G]`/`[P]` labels, shows G→P as green `+` instead of yellow `~`                                                                                                    |
| D-196 | Bug: No toast for global agent toggle — `toggleAgent` now guards against toggling pre-installed global agents from project scope, shows "Global agents cannot be changed from project scope" toast                                                                                                  |
| D-193 | Bug: Excluded global skill shows as selected in edit mode — fixed in v0.115.0: `currentSkillIds` now filters `!s.excluded` when building config skill IDs                                                                                                                                          |
| D-192 | Bug: Scope toggle G→P then deselect removes from global — fixed in v0.115.0: `detectConfigChanges` now diffs against `oldConfig.skills` instead of `currentSkillIds`                                                                                                                               |
| D-191 | Build step dual-scope badges — `secondaryScope` on `CategoryOption`, rendered as second G/P badge in `SkillTag`; computed from excluded+active configs with different scopes in `build-step-logic.ts`                                                                                               |
| D-190 | Bug: Build step allows deselecting global skills — fixed in v0.113.0/v0.114.0: `toggleTechnology` guard blocks deselection with toast                                                                                                                                                              |
| D-166 | Fix E2E try/finally blocks — removed all try/catch/finally from 16 test files app-wide; cleanup lifted to afterEach, errors handled with `.catch(() => {})` or `expect().rejects.toThrow()`                                                                                                        |
| D-177 | `uninstall --all` misses re-scoped plugins — already fixed: `uninstallPlugins` tries both primary scope (from config) and fallback scope (opposite) for each plugin, covering re-scoped entries                                                                                                     |
| D-173 | Agents may not auto-compile after init on Mac — closed: code review shows `fs.realpathSync()` resolves symlinks on both sides, compilation writes to explicit paths, no silent failure path found                                                                                                   |
| D-122 | Auto-update marketplace — already implemented: `ensureMarketplace()` calls `claudePluginMarketplaceUpdate()` when marketplace exists, both init and edit invoke it before plugin install                                                                                                            |
| D-200 | Info panel source mode changes — `SkillAgentSummary` now detects source changes via `prevSourceMap`, renders `~` prefix with transition label (e.g. "Agents Inc → Eject") for plugin↔eject changes                                                                                                 |
| D-195 | Auto-preselect agents from stack — `populateFromStack` derives `selectedAgents` from `Object.keys(stack.agents)`, merged with global preselections in `stack-selection.tsx`                                                                                                                         |
| D-194 | Stack grouping: explicit `group` property — added `group?: string` to `ResolvedStack`, rewrote `groupStacks()` to use `stack.group` instead of skill ID inference, tagged 8 React + 1 CLI stack in generated matrix                                                                                |
| D-189 | Visually distinguish installed global skills — lock icon (`🔒`) shown after display name for skills with `installed && scope === "global"` in `SkillTag` component                                                                                                                                 |
| D-183 | Agents step dual-scope badges + S hotkey guard — dual `[G][P]` badges via `secondaryScope` in `step-agents.tsx`, S hotkey guard in `wizard.tsx` blocks re-scoping global agents from project scope                                                                                                 |
| D-167 | Remove task IDs from describe blocks — already done in prior session; fixed `sourceTempDir = undefined` → `undefined!` reset pattern in `search-interactive.e2e.test.ts`                                                                                                                           |
| D-151 | E2E session-level timeout — `defaultTimeout` property on `TerminalSession` and `BaseStep`, threaded through wizard launch options, magic numbers moved to `TIMEOUTS` in constants.ts                                                                                                                |

---

## From TODO-deferred.md

| ID   | Task                                                                                 |
| ---- | ------------------------------------------------------------------------------------ |
| D-72 | Only show Agents Inc logo on the first init screen (not on edit or subsequent steps) |

---

## From TODO-refactor.md

| ID   | Task                                                                                                                                                                                                    |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R-04 | Eliminate redundant central config — remove `aliases` and `perSkill` from `default-rules.ts`, add `slug` to metadata, add `compatibleWith` groups, `setupPairs`, redesign `recommends` as flat picks    |
| R-08 | Unify resolve\* functions in matrix-loader — single function for resolving relationships (conflicts, compatibility, setup, requirements) instead of 5 separate functions with duplicate iteration logic |
